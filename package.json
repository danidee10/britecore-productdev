/* This package.json file is heroku specific,
The main package.json file is located in the frontend folder
*/

{
    "name": "britecore-productdev",
    "version": "1.0.0",
    "scripts": {
      "postinstall": "npm install --prefix frontend",
      "heroku-postbuild": "node frontend/build/build.js"
    }
  }